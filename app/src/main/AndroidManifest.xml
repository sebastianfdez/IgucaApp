<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.sebastianfernandez.iguca">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/iguca_logo"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/iguca_logo"
        android:supportsRtl="true"
        android:theme="@style/AppTheme"
        android:windowSoftInputMode="adjustResize">
        <activity
            android:name=".MainActivity"
            android:label="@string/app_name">
            <intent-filter>
                <action
                    android:name="android.intent.action.MAIN"
                    android:label="@string/app_name"
                    android:theme="@style/Theme.AppCompat.Light.NoActionBar.FullScreen" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".LoginActivity" />
        <activity android:name=".InstructionsActivity" />
        <activity android:name=".CoursesActivity" />
        <activity android:name=".CourseManualActivity" />
        <activity android:name=".CourseExamActivity" />
        <activity
            android:name=".SecretLogin"
            android:noHistory="true" />
        <activity android:name=".AdminListCompanies" />
        <activity android:name=".AdminCoursesList" />
        <activity android:name=".CourseExerciseMenu" />
        <activity android:name=".CourseExercises" />
        <activity android:name=".CourseExercicesSolutions" />
        <activity android:name=".CourseExamMenu" />
        <activity android:name=".CourseExamCase" />
        <activity android:name=".QuestionFile"></activity>
    </application>

</manifest>